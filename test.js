var script = document.createElement('script');script.type = "text/javascript";script.text = "/* eslint-disable */(function monkeyPatch(xhr) {  // Capture request before any network activity occurs:  var send = xhr.send;  function banana(xhrInstance) { // Example    console.log('Monkey RS: ' + xhrInstance.readyState);  }  xhr.send = function monkeySend() {    var rsc = this.onreadystatechange;    if (rsc) {      // "onreadystatechange" exists. Monkey-patch it      this.onreadystatechange = function onreadystatechange() {        banana(this);        return rsc.apply(this, arguments);      };    }    return send.apply(this, arguments);  };})(XMLHttpRequest.prototype);/* eslint-enable */";document.head.appendChild(script);
